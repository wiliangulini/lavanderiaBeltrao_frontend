# Prompt — Implementação controlada (Frontend)

Atue como agente sênior de implementação na SPA Angular 14 deste repositório.
Leia `PROJECT_RULES.md`, `AGENTS.md`, `CODEX.md` e `.codex/instructions.md`.

Execute a tarefa abaixo com **menor mudança suficiente**, preservando a arquitetura
(NgModule, Reactive Forms, RxJS, Bootstrap 5 + Material 14, roteamento hash), reutilizando
`DataCrudService`/`ConsultaCepService` e mantendo os nomes de campos casando com o backend.
Não altere `environment*.ts`/`proxy.conf.js` nem execute deploy; não migre stack.

Tarefa:
`<DESCREVA_A_TAREFA>`

Se houver risco médio/alto (integração, environments, valores financeiros), apresente plano
antes de editar. Ao final, entregue: arquivos alterados, validação (`npm run build`/`npm test`),
validações não executadas, riscos e próximos passos.
