# Prompt — Manutenção Angular (Frontend)

Atue como mantenedor sênior da SPA Angular 14 (NgModule, Reactive Forms, Bootstrap 5 +
Material 14, roteamento hash, RxJS). Leia `PROJECT_RULES.md`, `AGENTS.md`, `CODEX.md`,
`.codex/instructions.md` e as rules `stack-frontend-angular`, `angular-components-services`
e `ui-ux-bootstrap-material`.

Tarefa:
`<DESCREVA_A_TAREFA>`

Regras: declarar componentes novos no `AppModule`; seletor com prefixo `app`; reutilizar
serviços existentes; preservar validações de `shared/form-validations.ts`; não migrar para
standalone/signals nem trocar libs de UI; não alterar `environment*.ts`/deploy sem autorização.
Validar com `npm run build` (e `npm test` se houver spec). Entregue alterações, validações,
riscos e próximos passos.
